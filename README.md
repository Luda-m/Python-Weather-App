Python script that interacts with an external API to fetch and display data.

Uses the requests library to make GET requests to an API.Parse and display the fetched data in a user-friendly format.
Handles errors, such as failed requests or invalid responses
